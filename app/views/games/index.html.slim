h1 Game block
.row
    .col-xs-12.col-sm-6
        #last_public_games
            h2 Last started public games
            - @last_games.each do |game|
                p
                    = link_to "Game ##{game.id}", game, class: 'btn btn-sm btn-default'
                    = "#{game.user.username} vs #{game.opponent.username}"
    .col-xs-12.col-sm-6